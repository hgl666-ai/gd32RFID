# 自我笔记
# 7/20
##  项目介绍
    RFID标签接近设备时鉴权读取标签信息，GD32拿到标签信息后，对标签信息进行操作，成规定的40字节明文，然后通过FMSE加密，将密文返回给上位机。

## 加密流程
协议01走加密，协议02不走加密。

**如何获取se认证：**
上电后main() → app_protocol_init() → crypto_chip_init()
 ->fm_se_register()  ← 注册 I2C 驱动
        ↓
fm_open_device()  ← SE 上电
        ↓
fm_dev_power_on() ← 获取 ATR
认证成功获得s_se_authenticated=1，失败为0。此时为se认证失败状态。

### 协议01标签上传过程
RFID 轮询任务 (每 200ms)
  ↓
RequestA → Anticoll → Select → 读 3 块 → 序列化 40 字节明文
  ↓
crypto_chip_encrypt(tag_raw, 40, enc_buf, &enc_len)
  ↓
┌── 生产模式 (CRYPTO_PASSTHROUGH=0):
│   ↓
│   检查 s_se_authenticated
│     ├─ = 0 → DBG_PRINTF("[SE] not authenticated") → return 0 → 不上传 ❌
│     │
│     └─ = 1 (SE 已认证) → 继续:
│         ↓
│         构造 write_tlv[49 字节]: [C0 02 00 03][C1 02 00 00][C2 28][40字节明文]
│         ↓
│         write_se_data() → se_cmd.c → fmse_i2c.c → fmse_port.c → bsp_i2c.c
│         ↓                                                                 
│         I2C 把 49 字节 TLV 发给 SE 芯片                                   
│         ↓                                                                 
│         SE 内部 (假设): 接收明文 → 用 session_key 做 3DES 加密            
│         ↓                                                                 
│         构造 read_tlv[11 字节]: [C0 02 00 03][C1 02 00 00][C2 01 28]      
│         ↓                                                                 
│         get_se_data() → I2C 从 SE 读回 40 字节密文                        
│         ↓
│         校验密文长度 == 40
│         ↓
│         返回密文 → enc_buf
│
└── 透传模式 (CRYPTO_PASSTHROUGH=1):
    ↓
    memcpy(enc_buf, tag_raw, 40)
    ↓
    返回明文 → enc_buf

### MCU做3DES加密是否有必要
有必要，复旦微给的示例代码中出现了3DES加密的代码,MCU的’des.c‘采用了标准的3DES实现，SE芯片内部也是标准的3DES。mcu_uid[8]和mcu_com_key[16]的值来自于示例代码，硬编码在代码中，若SE芯片预制的key进行了修改，认证会失败，需要重新配置。
#### 认证流程
MCU 用 mcu_uid 构造随机数 → 发给 SE
                        → SE 用自己预置的 key 加密后返回 R1'
MCU 用 mcu_com_key 解密 R1' → 验证是否正确
                        → 正确：双方生成相同的 session_key
                        → 错误：返回 0xFFCE，认证失败

### MIFARE Classic 1K卡
代码读扇区1(块4/5/6)，`TAG_BLOCK_START=4` 在 bsp_fm17622.h。
换扇区改此值即可，每扇区4块。

# 7/21

##  Q & A
### 刷卡是否需要命令才能上传？
#### 答：不需要任何命令，main里轮询检查，卡片靠近自动上传协议01数据。
### 刷卡后上位机没收到数据解决
#### 答：①看启动日志 ②协议02的key是否写入 ③FM17622是否在线 ④卡片是MIFARE Classic 1K ⑤KEY与卡片KEYA匹配
### 三套密钥体系
#### 答：① 上位机下发的 KEY (16字节, 存 FLASH)--- KEY[0..5]  → MIFARE KeyA 认证卡片---KEY[6..15] → 预留 (未用) 
                                                  
② SE 双向认证凭证 (硬编码在 bsp_crypto.c)           
   └─ mcu_uid[8] + mcu_com_key[16]                  
   └─ 与上位机下发的 KEY 无关 

③ MIFARE 卡片内部的 KeyA (卡片厂商预置)                 
   └─ 由上位机生成, 通过协议02下发到 MCU  
###  切到生产模式是否打通加密？
#### 答：没有 ，代码路径走通了但 4 个假设都没验证（SE 在线/自动加密/密文长度=40/上位机能解密）。必须接上 SE 实测才能确认。

### FMSE的I2C地址
#### SDK移植时确认，代码中给的是写0xE2,读0xE3