# 固件更新说明（致甲方）

> 版本：V1.1
> 日期：2026-07-17

---

## 一、本次更新修复的问题

上一版固件存在以下问题，现已全部修复：

| 问题 | 原因 | 修复方式 |
|------|------|---------|
| 收不到UUID | 调试信息与协议数据共用同一串口，ASCII文本污染了二进制协议帧 | 关闭所有调试输出，串口只传输协议数据 |
| 收到的数据是乱码/夹杂文字 | 同上，串口收到的是 `[RFID] Card removed` 等文字混着协议帧 | 同上 |
| 调试信息一直打印 | RFID轮询任务周期性打印状态信息 | 删除周期性打印，仅保留错误日志（默认关闭） |
| 协议没法连通 | 串口数据被污染，上位机无法解析协议帧 | 修复污染后协议帧恢复正常 |

**核心改动**：新增调试输出开关（`DEBUG_ENABLE`），发布固件中设为关闭状态，串口（USART0, PA9/PA10）上只会有纯净的二进制协议帧，不再有任何ASCII文本干扰。

---

## 二、串口配置

| 参数 | 值 |
|------|-----|
| 波特率 | 115200 |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | 无 |
| TX引脚 | PA9 |
| RX引脚 | PA10 |

**重要**：使用串口工具测试时，请切换到 **HEX模式**（十六进制收发），不要用文本模式。

---

## 三、协议测试方法

### 指令1：查询主控UID

**上位机发送**（固定6字节）：
```
A5 5A 01 00 6B 03
```

**MCU应答**（20字节）：
```
A5 5A 01 0C [12字节UID] [CRC_H] [CRC_L]
```
其中 UID 是主控芯片的唯一ID（每块板子不同），CRC 随 UID 变化。

**示例**（假设 UID = `39 53 48 85 13 46 54 50 41 38 54 56`）：
```
A5 5A 01 0C 39 53 48 85 13 46 54 50 41 38 54 56 56 C1
```

---

### 指令2：写入16字节KEY

**上位机发送**（22字节）：
```
A5 5A 02 10 [16字节KEY数据] [CRC_H] [CRC_L]
```

**示例**（KEY = `01 02 03 04 05 06 07 08 09 0A 0B 0C 0D 0E 0F 10`）：
```
A5 5A 02 10 01 02 03 04 05 06 07 08 09 0A 0B 0C 0D 0E 0F 10 94 00
```

**MCU应答**（7字节）：
```
A5 5A 02 01 [结果] [CRC_H] [CRC_L]
```
- 结果 = `01`：写入成功 → `A5 5A 02 01 01 51 1B`
- 结果 = `00`：写入失败 → `A5 5A 02 01 00 91 DA`

---

### CRC校验说明

- 算法：CRC16-Modbus（多项式 0xA001，初值 0xFFFF）
- 校验范围：帧头 + 命令码 + 长度 + 数据（不含CRC本身）
- 字节序：大端（CRC高字节在前，低字节在后）

**查询UID命令的CRC是固定的**（`A5 5A 01 00` → CRC = `6B 03`），可直接复制使用。

**写入KEY命令的CRC需根据KEY数据计算**。如需自行计算，可使用在线 CRC16-Modbus 计算工具，输入 `A5 5A 02 10` + 16字节KEY数据，取结果的大端两个字即可。

---

## 四、测试步骤

1. 用 Keil 打开工程，确认 `Head/debug_config.h` 中 `DEBUG_ENABLE` 为 `0`
2. 编译并烧录固件到开发板
3. 连接串口：开发板 PA9(TX) → 串口工具 RX，开发板 PA10(RX) → 串口工具 TX，共地
4. 打开串口工具，配置 115200 / 8N1 / 无校验，切换到 **HEX收发模式**
5. 上电后，串口不应收到任何数据（无调试输出）
6. 发送查询UID命令 `A5 5A 01 00 6B 03`，应收到 `A5 5A 01 0C ...` 应答
7. 发送写入KEY命令（见上方示例），应收到 `A5 5A 02 01 01 51 1B`（成功）
8. 断电重新上电，再次查询UID确认KEY已持久存储（KEY写入不影响UID，但读卡功能需要KEY）

---

## 五、协议帧格式

```
| 帧头    | 命令码 | 长度 | 数据域      | CRC16      |
| A5 5A   | 1字节  | 1字节 | N字节       | 高字节 低字节 |
```

- 帧头固定 `A5 5A`
- 所有字节序为大端格式
- CRC16 校验范围覆盖 帧头 + 命令码 + 长度 + 数据

---

## 六、命令码速查

| 命令码 | 长度 | 方向 | 含义 |
|--------|------|------|------|
| 0x01 | 0x00 | 上位机→MCU | 查询主控UID |
| 0x01 | 0x0C | MCU→上位机 | UID应答（12字节） |
| 0x01 | 0x28 | MCU→上位机 | 上传标签数据（40字节） |
| 0x02 | 0x10 | 上位机→MCU | 写入KEY（16字节） |
| 0x02 | 0x01 | MCU→上位机 | KEY写入应答（1字节：00失败/01成功） |

---

## 七、注意事项

1. **KEY写入期间**：FLASH擦写约需20-50ms，期间MCU不响应串口命令。请等待KEY写入应答（`A5 5A 02 01 ...`）后再发送下一条命令。

2. **看门狗**：固件启用了200ms硬件看门狗，若程序异常会自动复位恢复。

3. **加密芯片**：若板载FMSE加密芯片，上电时会自动进行双向认证（约1-3秒）。认证期间不响应串口命令，请等待约3秒后再发送查询命令。认证失败时自动降级为明文透传模式，不影响协议02（密钥录入）功能。

4. **本地调试**：如开发人员需要查看调试日志，可将 `Head/debug_config.h` 中的 `DEBUG_ENABLE` 改为 `1` 重新编译。此时串口会输出调试文本（与协议帧混合），仅限开发调试使用，正式交付须改回 `0`。
