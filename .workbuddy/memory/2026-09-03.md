# 2026-09-03 工作日志

## 任务① 调试/生产模式配置整理 + 任务② 无输出问题取证固件

### 背景
- 用户烧录生产固件(DEBUG_ENABLE=0, CRYPTO_PASSTHROUGH=0)后, 甲方烧录了SE加密芯片,
  整板上电刷卡串口无任何输出, 实机也无反应。
- 已核实: 最近hex(07-21构建)为全静默生产版, 上传流水线有5个静默失败点
  (FM17622离线/KEY缺失/读卡失败/SE未认证/SE加密失败), 任一失败都静默return。

### 配置问题(任务①, 已修复)
1. CRYPTO_PASSTHROUGH 双重定义冲突: bsp_crypto.h #ifndef 默认=1, debug_config.h 强制=0,
   bsp_crypto.c/app_protocol.c 的 include 顺序导致宏重定义警告, 最终值依赖包含顺序。
   → 修复: bsp_crypto.h 删除默认定义, debug_config.h 成为唯一配置源;
     bsp_crypto.c/app_protocol.c 把 debug_config.h include 移到最前。
2. DBG_PRINTF 零参数变参宏警告 → 改为 DBG_PRINTF(...) printf(__VA_ARGS__) / ((void)0)。
3. DEBUG_ENABLE=0 时 se_online/calc_crc unused 警告 → (void)se_online + CRC_ERR块 #if 包裹。

### 取证与加固(任务②, 已实现)
- app_rfid_poll_task 各静默失败点加一次性/限频诊断打印:
  [RFID] FM17622 OFFLINE / new card UID / Anticoll FAIL / Select FAIL /
  KEY not stored / ReadTagData FAIL / encrypt failed (SE=AUTH/NOAUTH)
- crypto_chip_init 增加 [SE] ATR OK len=%u; 失败打印已有SW码
  (0x9000成功/0xFFCE凭证错/0xFFE2无ACK)。
- 生产模式(CRYPTO_PASSTHROUGH=0) SE未认证时每5s重试认证(原只在开机一次)。
- 防御加固: fm_i2c_recv_frame 数据域>64B即拒绝(防栈溢出); atr_buf/recv_buf 扩容64B;
  se_auth1/se_auth2/write_se_data/get_se_data 增加 rlen<2 下溢保护和3DES分组对齐检查。

### 构建验证(Keil UV4 CLI)
- 取证版(DEBUG_ENABLE=1): 0 Error, 8 Warning(均为库文件原有良性警告)。
- 生产版(DEBUG_ENABLE=0): 0 Error, 8 Warning。
- 重定义/零参数变参/unused 警告全部消除。
- 最终 hex: Objects\project.hex (取证版, 含 [SYS] Ready 等诊断串)。

### 遗留事项
- 候选根因A(SE凭证不匹配→0xFFCE): 用户暂按与SDK一致处理, 未改凭证; 取证版会打印SW码。
- 候选根因B(烧录全片擦除→KEY缺失): 取证版会打印 KEY not stored。
- 候选根因C(SE write/get流程4假设未验证: 自动加密/密文长度=40/前置se_active/上位机解密):
  暂未改动流程, 等取证输出后再定 (可能需补 get_se_uid/se_active 前置, 密文长度可能为48)。
- 桌面副本 C:\Users\Administrator\Desktop\RFID 与工作区代码曾完全一致;
  本次改动在工作区 D:\Project\RFID_M1\RFID, 需用户自行同步6个文件或直接用工作区工程。
- 正式交付: debug_config.h 中 DEBUG_ENABLE 改回 0。

## 任务一: 状态机逻辑审查 (logicprobe_verify, 用户确认模型后运行)

### 模型1: 协议解析状态机 (protocol.c, 7状态13转移)
- 通过: S1可达性 / S2无死锁 / S4确定性 / S6守卫完备 / S7不变量(帧头必须在数据态/CRC态之前,
  A5->5A->CMD 顺序) / A2 / A4 / A5边界 / A6 / A7无最短反例 / A10序列顺序。
- S3"闭合SCC无终态"= 解析器是永续反应式机器(帧结束后回S0等下一帧), 属设计行为非缺陷。
- S5/A1"64个未处理事件对"= 建模抽象产物: 真实代码每状态都消费任意字节并按值分发
  (S0的O分支覆盖所有非A5字节, S2任意字节即CMD), 不存在真正被忽略的字节。
- A3"顺序敏感"= 字节流解析器本质上顺序敏感(A5 5A必须按序), 属设计行为。
- 结论: 无功能性缺陷, 不改代码。

### 模型2: 刷卡去重锁存 (app_protocol.c, 2状态+UPLOAD守卫)
- 通过: S6守卫完备(补充显式false分支后) / S7 / 全部A检查无反例。
- 关键性质验证通过: SAME_CARD后 UPLOAD 被守卫阻断(不重复上传),
  只有 NEW_CARD 重新武装; NO_CARD 后回 ABSENT, 重新靠近可再次上传(设计意图)。
- S3/S5/A1/A3 的 fail 项均为永续锁存/事件不可能性/顺序敏感的设计行为, 非缺陷。
- 结论: 去重逻辑正确, 不改代码。

### 状态机审查观察项(非缺陷, 未改动)
1. Pack_Data_Frame 未限制 dataLen>250 (理论溢出s_tx_buf[256], 当前调用最大64B, 无实际风险)。
2. S1遇非A5/5A字节静默回S0不报FRAME_ERR (设计选择)。
3. 数据域无转义, 靠长度计数保护 (符合协议约定)。

## 2026-09-04 取证打印增强 (防刷屏版)
- bsp_crypto.c 加密成功打印增加明文/密文前8字节对比:
  `[SE] OK %uB->%uB P: XX.. C: XX..` (每张卡一次)
  P==C → SE未做变换(透传存取); P!=C → SE确实加密。
- app_protocol.c 防刷屏加固(用户提醒历史教训): [RX] Invalid byte 逐字节打印
  加限频 DBG_INVALID_PRINT_MAX=3, 收到合法帧后复位; 防止RX噪声刷屏阻塞命令收发。
- 取证版与生产版均编译通过: 0 Error / 8 Warning(库良性警告)。
- 最终取证hex: Objects\project.hex (含 P:/C: 对比打印)。
- 09-04 增: 开机摘要加 KEY=STORED/EMPTY 状态打印 (app_protocol_init 一行),
  用于电脑重烧后一眼确认第63页KEY是否被擦除 (0 Error / 3 Warning, hex已更新)。
- 重烧保住KEY的方法: Keil Flash Download 选 "Erase Sectors" 而非 "Erase Full Chip";
  或先用ST-Link整片读备份(0x08000000,0x10000); 丢了等治具重跑协议02可恢复同一KEY。

## 2026-09-07 F8P6板(GD32E230F8P6TR, TSSOP20)移植适配
- 背景: 甲方开发板芯片为 F8P6(TSSOP20, 64KB Flash/8KB SRAM), 非文档假设的C8T6(LQFP48)。
- 原理图事实(ziliao/GD32E230F8P6TR原理图描述.txt):
  8MHz晶振X2存在(PF0/PF1) → HXTAL时钟无需改;
  NFC_TX/NFC_RX = PA9/PA10(脚17/18) → 串口无需改;
  I2C1_SCL/SDA = PA0/PA1(脚6/7) → 原PB6/PB7不存在, 必须改;
  RFID_NPD = PA4(脚10, FM17622使能/复位脚, 低有效) → 必须拉高, 原代码未配置;
  RFID_IRQ = PA3(不用, 轮询寄存器); SYS_LED = PA5(不用); BOOT0默认低。
- 修改(2文件):
  1. Head/bsp_i2c.h: I2C_RCU/PORT/SCL/SDA 宏 GPIOB/PB6/PB7 → GPIOA/PA0/PA1。
  2. Bsp/bsp_fm17622.c: FM17622_Init 开头加 PA4 初始化: 拉低5ms复位→拉高10ms使能
     (加include bsp_systick.h)。
- 验证: 取证版与生产版均 0 Error / 8 Warning(库良性警告); 最终取证hex已生成。
- 注意: 本固件针对F8P6板; 旧C8板需把宏改回PB6/PB7。Keil器件仍为GD32E230C8
  (烧录算法同族兼容, 可改F8P6更规范)。上电等SE认证(最长4s)后再看串口。

## 2026-09-08 实板取证结果 + I2C总线扫描
- 板A: SE ATR OK len=15, 认证0xFFCE(凭证与SDK示例不匹配), KEY=STORED, FM17622 OFFLINE。
- 板B: SE=ONLINE(凭证=SDK示例值, 认证全通), KEY=EMPTY, FM17622 OFFLINE。
- 结论: GD32↔SE通讯两块板都通(引脚/驱动/认证代码正确); 0xFFCE=凭证问题;
  ONLINE有3DES随机数反解比对证明, 空白芯片不可能误报ONLINE。
- FM17622两块板都读不到版本 → 读卡器未上线是当前最靠前阻塞点。
- 新增 i2c_bus_scan(): bsp_i2c.c (遍历0x08~0x77打印ACK设备, DEBUG版有效),
  main.c 在 FM17622_Init 后调用。预期: SE=0x71(写0xE2), FM17622=0x28(写0x50)。
- 读取架构保持三芯片直连不变 (用户明确要求)。取证/生产版均 0 Error。
- 扫描实测: 总线只有 SE 0x71 应答, FM17622 无应答(全地址无0x28) → 电性不存在,
  硬件层问题(使能极性/供电/接线), 与固件I2C无关。
- 新增 FM17622_NpdDiag(): PA4高/低各做一次总线扫描+版本寄存器读取,
  判定RFID_NPD真实使能极性; 结束恢复PA4高。取证/生产版均0 Error。
- 09-08 万用表+原理图确认板上真实拓扑(决定性):
  MCU PA0/PA1(I2C1) ↔ FM15L013(SE) pin5/pin1 (从机I2C);
  SE pin6(PA0/GPIO0/CT_CLK)=RFID_SCL, pin3(PA6/GPIO4/CT_HRS)=RFID_SDA
  → FM17622 pin31/pin24: 读卡器挂在SE的GPIO主机口上, MCU无法直连读卡器!
  RFID_NPD(PA4)→FM17622 pin6 NPD + SE pin7(PA1/CT_RST);
  RFID_IRQ(PA3)←FM17622 pin23 IRQ + SE pin2(PA7/I2C_IRQ) 并联。
  6.6kΩ之谜: PA0→R34(4.7k)→3.3V轨→R6(2k)→FM17622 SCL = 6.7kΩ, 蜂鸣档误判。
  结论: 这块板是SDK原生架构 MCU→SE→FM17622, 读卡必须走SE
  (se_cmd.c 已移植 set_tag_reader/get_tag_uid/tag_active/get_tag_data)。
- 待甲方确认后才实施SE读卡改造(用户已确认):
  1. get_tag_data返回的数据格式(是否40字节结构);
  2. 协议02下发的16字节KEY在SE架构中的角色;
  3. write_se_data/get_se_data真实语义(自动加密?读回密文?);
  4. FM17622地址(ADR2:0全拉高)与TVDD供电来源。
  待甲方: 完整原理图/FM15L013应用手册/SE生产凭证(板A 0xFFCE)/测试卡。

## 2026-09-08(下午) SE读卡主流程已实现
- 用户拍板: 数据格式先按40字节结构; KEY仍存FLASH做门控; SE自动加密沿用;
  地址自动尝试{0x28,0x2F,0x2E} (set_tag_reader成功者胜出); 凭证保持SDK示例值。
- debug_config.h 新增 RFID_READ_VIA_SE (1=SE读卡F8P6板, 0=直连旧C8板)。
- app_protocol.c 轮询任务双路径: SE路径 set_tag_reader→get_tag_uid(ATQA2+UID7+SAK2)
  →去重→KEY门控→tag_active(Tag_SigCh_Key)→get_tag_data(取前40B)→加密→上传。
- bsp_fm17622.c: SE模式下FM17622_Init跳过I2C配置只保留PA4使能脉冲;
  main.c: SE模式下跳过CheckComm。
- 编译验证三种组合均 0 Error: SE+DEBUG(10W)/SE+PROD(10W)/直连+DEBUG(9W)。
- 最终取证hex: Objects\project.hex (SE读卡模式)。
- 测试要点: 先用协议02写任意KEY(板B是EMPTY会被门控), 再贴卡看
  set_tag_reader/tag_active/get_tag_data 的SW码。

## 2026-09-08(傍晚) 0x9EFA 排查 + 探针 + TLV修复
- 实测: set_tag_reader 三地址(0x28/0x2F/0x2E)均返回 SW=0x9EFA (SE应用层拒绝,
  传输与认证均正常) → 非地址问题; 每200ms重试导致刷屏。
- 修复1(防刷屏): 读卡器配置失败后每5秒重试一轮 (s_last_setup_ms)。
- 新增探针(每轮重试前): get_se_uid → se_active(0x0100) → write_se_data(KEY TLV)
  各打SW, 验证"KEY解锁读卡"假设(协议原文: 主板主控通过KEY进行认证即可识别三密标签)。
- 修复2(TLV差一bug, 编译器fortify告警发现): 写入TLV头实为10字节
  (C0 02 00 03|C1 02 00 00|C2 len), 原代码数组9+len且inlen传9+len, 少传1字节:
  1) app_protocol.c 探针KEY TLV: 数组与inlen均改10+16;
  2) bsp_crypto.c 加密write TLV: 数组9+64→10+64, inlen 9+plain_len→10+plain_len
  (SDK se_wdat[14]=头10B+4B数据 为证)。此bug曾潜伏在加密写流程中。
- 三种配置编译验证: SE+取证0E/10W, SE+生产0E/10W, 直连+取证0E/9W。
- 0x9EFA 确切含义待甲方错误码表; 下一步按探针SW判读。
- 注意: 协议01文档(3.16版)定义上传帧为40字节明文结构, 与"SE加密后上传"实现不一致,
  需师父/甲方确认上位机收到后按明文解析还是先解密。
- FM15L013技术手册(ziliao)= 加密芯片(SE)硬件手册, 不含0x76/0x78指令定义(指令在SDK层);
  板三芯片: GD32E230(主控)/FM15L013(SE)/FM17622(RFID读卡)。
- pdftotext提取文本存 ziliao\FM15L013_extracted.txt。
