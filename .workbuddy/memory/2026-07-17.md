# 2026-07-17 工作日志

## 甲方反馈问题修复（重点协议02密钥录入）

### 审查结论
- 完整审查项目代码 + 两个协议xlsx + 4个历史md文档
- 定位甲方4个问题（①收不到UUID ②协议不一样 ③调试信息一直打印 ④协议没法连通）的根因：**printf与协议数据共用USART0，ASCII调试文本污染二进制协议帧**
- 02协议代码逻辑本身正确（CRC/CMD/LEN全部与表格示例一致），"连不通"纯粹是printf污染所致

### 修复内容
1. **新建 `Head/debug_config.h`**：DEBUG_ENABLE总开关，0=关闭(发布) 1=开启(调试)；定义DBG_PRINTF宏
2. **`Bsp/bsp_usart.c`**：fputc加 #if DEBUG_ENABLE 保护，关闭时不向USART0发送任何字节（双保险）
3. **`Bsp/app_protocol.c`**：27处 printf→DBG_PRINTF；标签上传改用 APP_CMD_UPLOAD_TAG(0x01)
4. **`Bsp/bsp_crypto.c`**：8处 printf→DBG_PRINTF
5. **`User/main.c`**：15处 printf→DBG_PRINTF；**看门狗init从app_protocol_init之后移到bsp_i2c_init之后**（甲方连了加密芯片，SE认证可能卡4s+，看门狗必须先启动防死锁）
6. **`Head/app_protocol.h`**：APP_CMD_UPLOAD_TAG从0x28(误把LEN当CMD)改为0x01(单天线左侧)

### 验证结果
- 02协议CRC全部验证通过：查询UID→0x6B03、KEY写失败→0x91DA、KEY写成功→0x511B，与协议示例字节流完全一致
- 单天线确认，标签上传用CMD=0x01符合协议01

### 遗留事项（需向甲方/SE侧确认）
- SE内部是否自动加密（write_se_data后get_se_data读回的是密文还是明文）——question.md问题4未解决
- SE认证用硬编码mcu_uid/mcu_com_key，与上位机下发的KEY无关（设计分离，需确认是否符合预期）
- 启用SE加密后密文长度可能≠40，与协议01的LEN=40冲突

### 产出文件
- `PROTOCOL_FIX_REPORT.md` — 完整修复报告
- `固件更新说明_致甲方.md` — 面向甲方的问题说明+测试方法+HEX命令示例

### 二次精简（同日）
- 进一步删除 app_rfid_poll_task 中无关紧要的周期性DBG_PRINTF：
  - 删除 "[RFID] Card removed"（无卡时周期打印）
  - 删除 "[RFID] Card detected, UID: ..."（检测到卡时打印）
  - 删除 "[RFID] KEY not stored, skip tag read"
  - 删除 "[RFID] Tag data read OK"
- 保留：协议命令日志([RX]/[TX]/[KEY]) + 错误日志(Anticoll/Select/Read/Encrypt failed)
- 确认：全项目无遗漏的printf调用(bsp_fm17622.c/gd32e23x_it.c均无)
- 确认：协议收发路径不经过fputc(uart_send_data直接写USART0 DR寄存器)，DEBUG_ENABLE=0时协议命令能正确回复
